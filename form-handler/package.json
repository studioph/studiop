{
  "name": "form-handler",
  "description": "Contact form handler for AWS Lambda",
  "scripts": {
    "lint": "eslint .",
    "start": "npm run shell",
    "layer": "npm install && mkdir -p nodejs && cp -r node_modules nodejs/ && zip -r layer.zip nodejs"
  },
  "engines": {
    "node": "14"
  },
  "dependencies": {
    "juice": "^6.0.0",
    "nodemailer": "^6.6.1",
    "pug": "^3.0.2"
  },
  "devDependencies": {
    "eslint": "^5.12.0",
    "eslint-plugin-promise": "^4.0.1"
  },
  "private": true
}
